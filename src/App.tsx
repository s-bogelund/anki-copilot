import { Button } from '@/components/ui/button';

function App() {
	return (
		<div className="flex min-h-svh flex-col items-center justify-center">
			<Button className="hover:cursor-pointer active:scale-95">Click me</Button>
		</div>
	);
}

export default App;
